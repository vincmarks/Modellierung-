1. The code has been written in Julia 1.12

2. To reproduce the plots open a Julia repl and execute
    ```julia
        include("sheet_2/sheet2.jl")
    ```

3. The plots will be saved in the `Plots` folder.